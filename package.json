{
  "name": "knowledge-repository",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev src --host",
    "docs:build": "vitepress build src --outDir dist/human",
    "docs:preview": "vitepress preview src"
  },
  "devDependencies": {
    "vitepress": "latest",
    "vue": "^3.4.0"
  }
}
